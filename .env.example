# Expensify Integration Server credentials (generated at expensify.com by the
# credential owner — should be an approver account: Purvi or Erica).
EXPENSIFY_PARTNER_USER_ID=
EXPENSIFY_PARTNER_USER_SECRET=

# Optional — enables the LLM categorization fallback.
ANTHROPIC_API_KEY=
